let x = 3.14;
x = Math.round(x) // Returns a supplied numeric expression rounded to the nearest integer.

x = Math.floor(x) // Returns the greatest integer less than or equal to its numeric argument. ROUND UP A VALUE


x = Math.ceil(x) // number Returns the smallest integer greater than or equal to its numeric argument. ROUNDS UP 

console.log(x);

let a = 9;

a = Math.pow(a, 4) // Returns the value of a base expression taken to a specified power. THIS RAISE TO THE POWER OF 4

console.log(a);

a = Math.sqrt(a)

console.log(a);

a = Math.abs(a) // (method) Math.abs(x: number): number Returns the absolute value of a number (the value without regard to whether it is positive or negative). For example, the absolute value of -5 is the same as the absolute value of 5.

console.log(a);


let c = 4.2;
let d = 23;
let e = 23.2
let maximum;
let minimum;

maximum = Math.max(c, d, e)
console.log(maximum);
minimum = Math.min(c, d, e)
console.log(minimum);

a = Math.PI // (property) Math.PI: number Pi. This is the ratio of the circumference of a circle to its diameter.

console.log(a);
